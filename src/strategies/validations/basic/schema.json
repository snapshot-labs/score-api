{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/Validation",
  "definitions": {
    "Validation": {
      "title": "Basic validation",
      "type": "object",
      "properties": {
        "minScore": {
          "title": "Minimum score",
          "type": "number",
          "exclusiveMinimum": 0
        },
        "strategies": {
          "title": "Strategies",
          "type": "array",
          "items": {
            "title": "Strategy",
            "type": "object",
            "default": {
              "name": "ticket",
              "network": "1",
              "params": {
                "symbol": "DAI"
              }
            },
            "properties": {
              "name": {
                "type": "string"
              },
              "network": {
                "type": "string"
              },
              "params": {
                "type": "object"
              }
            },
            "required": ["name", "params"]
          }
        },
        "useLatestBlock": {
          "title": "Validate at the latest block",
          "description": "Check the strategies at the current block instead of the proposal snapshot block, so users who receive tokens after the proposal was created can still vote. Only use with non-transferable tokens (SBTs): with a transferable token the same token can be moved between wallets to pass validation multiple times. Cannot be combined with strategies that define start or end.",
          "type": "boolean"
        }
      },
      "required": ["minScore"],
      "additionalProperties": false
    }
  }
}
