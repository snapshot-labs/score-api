# RBNT Balance Strategy

This strategy returns the Native token balances as voting power

## Parameters

- `symbol` (optional): Token symbol for display purposes (e.g. "RBNT")

## Examples

```json
{
  "name": "rbnt-balance",
  "params": {
    "symbol": "RBNT"
  }
}
```
